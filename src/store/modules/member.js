import * as types from '../mutation-types'
import api from '../../api'

const state = {
    id: '',
    name: '',
    schedule: {
      intervals: {}
    }
}

const getters = {
  
}

const mutations = {
    
}

const actions = {
    
}

export default {
  state,
  getters,
  actions,
  mutations
}