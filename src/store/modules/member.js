import * as types from '../mutation-types'
import api from '../../api'

const state = {
    member: {}
}

const mutations = {
    
}

const actions = {
    
}

export default {
  state,
  getters,
  actions,
  mutations
}